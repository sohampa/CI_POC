pipeline {
    agent any

//    comment added 

    stages {
        stage('Build') {
            steps {
                // Get some code from a GitHub repository
                git 'https://github.com/sohampa/CI_POC.git'
            }
        }
    }
}
